# Copy to .env (or set in Vercel).
DATABASE_URL="file:./prisma/dev.db"
PORTAL_PASSWORD="kdirection-portal"
PORTAL_SECRET="change-me-in-production-kdirection"

# Later, when a Kebu/K-Direction Supabase project exists, switch DATABASE_URL
# to the Postgres URL and apply supabase/migrations/. Do not use Rect Sound's DB.
